class Solution(object):
    def minMoves2(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        moves=0
        nums.sort()
        tar=len(nums)//2
        for i in range(len(nums)):
            moves+=(abs(nums[i]-nums[tar]))
        return moves

        

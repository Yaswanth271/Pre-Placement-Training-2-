class Solution(object):
    def reverseWords(self, s):
        splitedstring=s.split()
        splitedstring.reverse()
        result=" ".join(splitedstring)
        print(result)
        return result
